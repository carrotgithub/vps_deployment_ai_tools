# LiteLLM 环境变量配置文件
# 复制此文件为 .env 并填入实际的密钥和配置

# ===========================================
# LiteLLM 核心配置
# ===========================================

# 主 API 密钥 - 用于访问 LiteLLM 代理服务器
# 这是访问 LiteLLM 服务的认证密钥
LITELLM_MASTER_KEY=sk-1234567890-replace-with-secure-key

# ===========================================
# 数据库配置
# ===========================================

# PostgreSQL 数据库密码
POSTGRES_PASSWORD=your-secure-postgres-password

# PostgreSQL 完整连接 URL（如果使用外部数据库）
# DATABASE_URL=postgresql://litellm:password@host:5432/litellm

# ===========================================
# Redis 缓存配置
# ===========================================

# Redis 密码（留空表示无密码）
REDIS_PASSWORD=

# 如果使用外部 Redis，配置以下参数
# REDIS_HOST=localhost
# REDIS_PORT=6379

# ===========================================
# OpenAI 配置
# ===========================================

# OpenAI API 密钥
OPENAI_API_KEY=sk-your-openai-api-key

# 可选：OpenAI API 基础 URL（用于代理或自定义端点）
# OPENAI_API_BASE=https://api.openai.com/v1

# 可选：OpenAI 组织 ID
# OPENAI_ORGANIZATION=org-...

# 多个 API 密钥用于负载均衡
# OPENAI_API_KEY_1=sk-key-1
# OPENAI_API_KEY_2=sk-key-2
# OPENAI_API_KEY_3=sk-key-3

# ===========================================
# Anthropic Claude 配置
# ===========================================

# Anthropic API 密钥
ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key

# 可选：Anthropic API 基础 URL
# ANTHROPIC_API_BASE=https://api.anthropic.com

# ===========================================
# Google Gemini 配置
# ===========================================

# Google AI (Gemini) API 密钥
GEMINI_API_KEY=your-gemini-api-key

# ===========================================
# Azure OpenAI 配置
# ===========================================

# Azure OpenAI API 密钥
AZURE_API_KEY=your-azure-api-key

# Azure OpenAI 端点
AZURE_API_BASE=https://your-resource.openai.azure.com/

# Azure OpenAI API 版本
AZURE_API_VERSION=2024-02-15-preview

# Azure 部署名称（如果需要）
# AZURE_DEPLOYMENT_NAME=gpt-4

# ===========================================
# AWS Bedrock 配置
# ===========================================

# AWS 访问密钥
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# AWS_REGION_NAME=us-east-1

# ===========================================
# Cohere 配置
# ===========================================

# Cohere API 密钥
# COHERE_API_KEY=your-cohere-api-key

# ===========================================
# Hugging Face 配置
# ===========================================

# Hugging Face API Token
# HUGGINGFACE_API_KEY=hf_your-token

# ===========================================
# Replicate 配置
# ===========================================

# Replicate API Token
# REPLICATE_API_TOKEN=r8_your-token

# ===========================================
# Together AI 配置
# ===========================================

# Together AI API 密钥
# TOGETHERAI_API_KEY=your-together-api-key

# ===========================================
# Ollama 配置（本地模型）
# ===========================================

# Ollama API 基础 URL
# OLLAMA_API_BASE=http://localhost:11434

# ===========================================
# 日志和监控配置
# ===========================================

# 日志级别: DEBUG, INFO, WARNING, ERROR, CRITICAL
LITELLM_LOG=INFO

# 启用详细日志
SET_VERBOSE=True

# 启用 JSON 格式日志
# JSON_LOGS=True

# ===========================================
# Langfuse 追踪配置（可选）
# ===========================================

# Langfuse 公钥
# LANGFUSE_PUBLIC_KEY=pk-lf-...

# Langfuse 密钥
# LANGFUSE_SECRET_KEY=sk-lf-...

# Langfuse 主机
# LANGFUSE_HOST=https://cloud.langfuse.com

# ===========================================
# Prometheus 监控配置
# ===========================================

# 启用 Prometheus 指标
# PROMETHEUS_PORT=9090

# ===========================================
# Slack 告警配置（可选）
# ===========================================

# Slack Webhook URL（用于告警通知）
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/...

# ===========================================
# 预算和速率限制配置
# ===========================================

# 全局速率限制（每分钟请求数）
# GLOBAL_RPM=1000

# 全局 Token 限制（每分钟 Token 数）
# GLOBAL_TPM=100000

# 全局预算（美元）
# GLOBAL_MAX_BUDGET=1000.0

# 预算时长（例如：30d, 7d, 24h）
# BUDGET_DURATION=30d

# ===========================================
# 安全配置
# ===========================================

# 允许的 CORS 源（逗号分隔）
# ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com

# 启用 API 密钥加密
# ENCRYPT_API_KEYS=True

# 加密密钥（32 字符）
# ENCRYPTION_KEY=your-32-character-encryption-key

# ===========================================
# 高级配置
# ===========================================

# 工作进程数
# NUM_WORKERS=4

# 请求超时（秒）
# REQUEST_TIMEOUT=600

# 最大并发请求数
# MAX_PARALLEL_REQUESTS=100

# 启用 UI 界面
# ENABLE_SWAGGER_UI=True

# 禁用遥测数据收集
# LITELLM_TELEMETRY=False

# ===========================================
# 代理配置（如果服务器需要代理访问外网）
# ===========================================

# HTTP 代理
# HTTP_PROXY=http://proxy.example.com:8080

# HTTPS 代理
# HTTPS_PROXY=http://proxy.example.com:8080

# 不使用代理的地址
# NO_PROXY=localhost,127.0.0.1

# ===========================================
# 使用说明
# ===========================================

# 1. 复制此文件为 .env：
#    cp .env.example .env
#
# 2. 根据需要修改配置值
#
# 3. 确保 .env 文件权限安全：
#    chmod 600 .env
#
# 4. 使用 Docker Compose 启动：
#    docker-compose up -d
#
# 5. 查看日志确认启动成功：
#    docker-compose logs -f litellm
#
# 6. 测试 API：
#    curl http://localhost:4000/health

# ===========================================
# 注意事项
# ===========================================

# - 不要将 .env 文件提交到版本控制系统
# - 使用强密码和随机生成的密钥
# - 定期轮换 API 密钥
# - 在生产环境中使用 HTTPS
# - 限制数据库和 Redis 的网络访问
# - 定期备份数据库
# - 监控 API 使用量和成本
